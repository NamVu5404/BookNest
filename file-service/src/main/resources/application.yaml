spring:
  application:
    name: file-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/file-service?authSource=admin
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  port: 8084
  servlet:
    context-path: /file

app:
  file:
    storage-dir: F:\uploads
    urlPrefix: http://localhost:8888/api/v1/file/media/download/
